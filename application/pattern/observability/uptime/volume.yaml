# kubectl -n observability apply -f volume.yaml
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: uptime-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 1Gi